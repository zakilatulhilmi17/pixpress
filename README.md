# pixpress
A simple but smart image batch compressor built with Python and Pillow. Supports JPG, PNG, and WebP — and it's clever enough to skip files that would get bigger after compression. Works on Windows, Linux, and macOS.
